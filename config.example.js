// Copy this file to `config.js` and paste your keys.
// Tip: Always restrict keys to your domain(s) in provider consoles.

window.APP_CONFIG = {
  // Weatherbit — current weather panel + overlay label
  WEATHERBIT_API_KEY: "YOUR_WEATHERBIT_API_KEY",
  // TomTom — POI search, routing, traffic flow/incident data
  TOMTOM_API_KEY: "YOUR_TOMTOM_API_KEY",
  // TransLink RTTI — live arrivals for Vancouver (optional)
  TRANSLINK_API_KEY: "YOUR_TRANSLINK_API_KEY",
};
